// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://enock-2000.github.io',
  base: '/Swag/',
  output: 'static',
});
